name: Build and Push Docker Image (1.4.5.6)

on:
    push:
        branches:
            - main
        paths:
            - "1.4.5.6/**"
            - ".github/workflows/build-and-push-docker-image.yml"
    workflow_dispatch:

jobs:
    build-and-push:
        runs-on: ubuntu-latest

        permissions:
            contents: read
            packages: write

        strategy:
            fail-fast: false
            matrix:
                variant:
                    - name: official
                      dir: ./1.4.5.6/official
                      tag-suffix: ""
                    - name: tshock
                      dir: ./1.4.5.6/tshock
                      tag-suffix: "tshock-"

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            - name: Log in to GitHub Container Registry (GHCR)
              uses: docker/login-action@v3
              with:
                  registry: ghcr.io
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Log in to ACR
              uses: docker/login-action@v3
              with:
                  registry: ${{ secrets.ALIYUN_REGISTRY }}
                  username: ${{ secrets.ALIYUN_REGISTRY_USER }}
                  password: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}

            - name: Prepare Image Tags
              id: prep
              run: |
                  OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
                  DATE=$(date +'%Y-%m-%d')
                  echo "owner=$OWNER" >> $GITHUB_OUTPUT
                  echo "date=$DATE" >> $GITHUB_OUTPUT

            - name: Build and push
              uses: docker/build-push-action@v6
              with:
                  context: ${{ matrix.variant.dir }}
                  push: true
                  provenance: false
                  tags: |
                      ghcr.io/${{ steps.prep.outputs.owner }}/terraria:${{ matrix.variant.tag-suffix }}latest
                      ghcr.io/${{ steps.prep.outputs.owner }}/terraria:${{ matrix.variant.tag-suffix }}${{ steps.prep.outputs.date }}
                      ${{ secrets.ALIYUN_REGISTRY }}/${{ secrets.ALIYUN_REGISTRY_NAMESPACE }}/terraria:${{ matrix.variant.tag-suffix }}latest
                      ${{ secrets.ALIYUN_REGISTRY }}/${{ secrets.ALIYUN_REGISTRY_NAMESPACE }}/terraria:${{ matrix.variant.tag-suffix }}${{ steps.prep.outputs.date }}
                  cache-from: type=gha,scope=${{ matrix.variant.name }}
                  cache-to: type=gha,mode=max,scope=${{ matrix.variant.name }}